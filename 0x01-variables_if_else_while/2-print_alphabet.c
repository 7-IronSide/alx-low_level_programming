#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
char i;
char c;
c = '\n';

	for (i = 'a'; i <= 'z' ; i++)
	{
	putchar(i);
	putchar(c);
	}
return (0);
}

